# 2018-02-05
购物车流程：
    1. 商品列表
    2. 选择商品（用户）（可以加入购物车）
    3. 结账（确认支付）

类型：
    不可变: 整型（数字）、字符串、元组。（保证唯一性）
    可  变: 列表、字典

字典：
    两大特点：
        1. 无序
        2. 键唯一
    创建(两种方法)
        1.
            dic = {'name': 'stanley', 'age': 24, 'hobby': 'girl','is_handsome':True}
            print(dic['name'])
        2.
            dic = dict((('name','stanley'),('age',24)))
            print(dic)